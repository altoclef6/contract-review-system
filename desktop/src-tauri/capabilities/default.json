{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-default",
  "description": "Minimal capability set for the Contract Review desktop window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

