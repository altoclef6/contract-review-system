# 项目长期开发计划

更新时间：2026-07-15

## 总体目标

保留现有 FastAPI 后端与 Vue 3 Web 端，在独立目录 `harmony_app/` 中新增 HarmonyOS NEXT 原生客户端。所有阶段遵循小步、可运行、可验证、可回滚原则。

## 当前基线

- 后端：FastAPI、Pydantic、SQLAlchemy、Alembic、LangGraph、多 Agent、Celery/Redis。
- Web：Vue 3、TypeScript、Vite、Pinia、Element Plus、ECharts。
- 数据：生产方向为 PostgreSQL；本地部分业务仍使用 `data/` 下 JSON 文档存储。
- 审查：同步 `/reviews` 与异步 `/review-tasks` 两套入口并存。
- 报告：JSON、DOCX、PDF、Markdown、XLSX。

## 阶段计划

### 阶段一：项目分析（已完成）

目标：确认结构、启动方式、核心流程、API、配置、风险及鸿蒙复用边界。

验收：能够从代码说明完整审查链路，并列出鸿蒙端所需接口和缺口。

### 阶段二：文档与计划基线（本轮完成）

目标：建立 PLAN、TODO、PROGRESS、QUESTIONS、DECISIONS、API_INVENTORY。

验收：文档互相一致；不修改业务代码；API 清单来自当前 OpenAPI。

依赖：阶段一。

### 阶段三：移动端 API 契约确认

目标：用测试客户端或实际服务逐项验证登录、用户、上传、异步任务、结果、风险、历史和报告接口。

验收：保存脱敏示例请求/响应；明确 Bearer 鉴权、multipart 格式、统一错误结构、超时与轮询策略。

依赖：用户确认移动端首版流程及接口选择。

### 阶段四：HarmonyOS 工程骨架

目标：创建 `harmony_app/`，搭建路由、主题、模型、网络层、会话存储、错误/加载/空状态。

验收：DevEco Studio 可打开并真实编译；页面骨架可独立跳转；API 地址集中配置。

依赖：阶段三、可用 DevEco Studio/HarmonyOS SDK。

### 阶段五：静态页面

顺序：登录 → 首页 → 上传 → 审查进度 → 结果 → 风险详情 → 历史 → 报告 → 设置。

验收：每页具有正常、加载、空数据、错误状态，不只有占位文字。

### 阶段六：真实接口接入

顺序：登录/用户 → 合同创建与上传 → 发起异步审查 → 轮询进度 → 获取结果/风险 → 历史 → 报告。

验收：每个接口均覆盖成功、业务失败、超时、断网、登录失效和重复点击。

### 阶段七：测试与修复

目标：完成 ArkTS 类型、路由、网络、上传、长文本、不同屏幕和异常路径检查。

验收：真实编译；关键流程有自动化或明确的手工验证记录。

### 阶段八：求职展示优化

目标：补充截图、演示流程、架构图、README、启动说明、常见问题和面试讲解材料。

验收：演示不暴露密钥或真实合同，项目贡献边界真实可说明。

